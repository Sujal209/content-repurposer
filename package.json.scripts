{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "npm run type-check && npm run lint",
    "clean": "rm -rf .next",
    "deploy": "npm run test && npm run build"
  }
}
